# modules

## 综述
该目录用于集中存放可复用模块实现，并按模块名称划分子目录。

## 文件列表
- chrome.test.js: 功能：实现模块逻辑。责任：为上层提供可复用能力或入口。内部结构：头部说明“Chrome 模块测试 测试模块接口验证和基本功能”，导出符号：（未匹配到明显的导出符号）。
- chrome_selector_sanitization.test.js: 功能：实现模块逻辑。责任：为上层提供可复用能力或入口。内部结构：头部说明“Chrome 选择器清理属性测试 Feature: chrome-selector-sanitization 使用 fast-check 进行属性测试，验证 _sanitizeSelector 方法的正确性”，导出符号：（未匹配到明显的导出符号）。
- modules.md: 功能：本目录说明文档。责任：描述目录综述、文件列表与子目录列表。内部结构：包含“综述 / 文件列表 / 子目录列表”三部分。

## 子目录列表
- （无）
